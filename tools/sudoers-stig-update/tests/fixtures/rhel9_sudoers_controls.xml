<?xml version="1.0" encoding="utf-8"?>
<Benchmark xmlns="http://checklists.nist.gov/xccdf/1.1" id="RHEL9_STIG_V2R7_TEST_FIXTURE">
<title>Trimmed test fixture: real DISA rhel9 RHEL 9 STIG V2R7 (05 Jan 2026) sudo-W04 directive Groups (authenticate / pw_family / timestamp_timeout) plus 1 NOPASSWD (sudo-W01/W05) decoy Group. check-content and fixtext are verbatim; title/description/reference/ident stripped (the parser never reads them).</title>
<!-- authenticate -->
<Group id="V-258086"><Rule id="SV-258086r1102063_rule" weight="10.0" severity="medium"><version>RHEL-09-432025</version><title>RHEL 9 must require users to reauthenticate for privilege escalation.</title><fixtext fixref="F-61751r926244_fix">Configure RHEL 9 to not allow users to execute privileged actions without authenticating.

Remove any occurrence of "!authenticate" found in "/etc/sudoers" file or files in the "/etc/sudoers.d" directory.

$ sudo sed -i '/\!authenticate/ s/^/# /g' /etc/sudoers /etc/sudoers.d/*</fixtext><check system="C-61827r1102062_chk"><check-content>Verify that "/etc/sudoers" has no occurrences of "!authenticate" with the following command:

$ sudo egrep -iR '!authenticate' /etc/sudoers /etc/sudoers.d/

If any occurrences of "!authenticate" are returned, this is a finding.</check-content></check></Rule></Group>
<!-- pw_family -->
<Group id="V-258085"><Rule id="SV-258085r1045173_rule" weight="10.0" severity="medium"><version>RHEL-09-432020</version><title>RHEL 9 must use the invoking user's password for privilege escalation when using "sudo".</title><fixtext fixref="F-61750r926241_fix">Define the following in the Defaults section of the /etc/sudoers file or a single configuration file in the /etc/sudoers.d/ directory:

Defaults !targetpw
Defaults !rootpw
Defaults !runaspw</fixtext><check system="C-61826r1045172_chk"><check-content>Verify that the sudoers security policy is configured to use the invoking user's password for privilege escalation with the following command:

$ sudo egrep -ir '(!rootpw|!targetpw|!runaspw)' /etc/sudoers /etc/sudoers.d/ | grep -v '#'

/etc/sudoers:Defaults !targetpw
/etc/sudoers:Defaults !rootpw
/etc/sudoers:Defaults !runaspw

If no results are returned, this is a finding.

If results are returned from more than one file location, this is a finding.

If "Defaults !targetpw" is not defined, this is a finding.

If "Defaults !rootpw" is not defined, this is a finding.

If "Defaults !runaspw" is not defined, this is a finding.</check-content></check></Rule></Group>
<!-- timestamp_timeout -->
<Group id="V-258084"><Rule id="SV-258084r1050789_rule" weight="10.0" severity="medium"><version>RHEL-09-432015</version><title>RHEL 9 must require reauthentication when using the "sudo" command.</title><fixtext fixref="F-61749r1045170_fix">Configure RHEL 9 to reauthenticate "sudo" commands after the specified timeout:

Add the following line to "/etc/sudoers" or a file in "/etc/sudoers.d":

Defaults timestamp_timeout=0</fixtext><check system="C-61825r1045169_chk"><check-content>Verify RHEL 9 requires reauthentication when using the "sudo" command to elevate privileges with the following command:

$ sudo grep -ir 'timestamp_timeout' /etc/sudoers /etc/sudoers.d/

/etc/sudoers:Defaults timestamp_timeout=0

If results are returned from more than one file location, this is a finding.

If "timestamp_timeout" is set to a negative number, is commented out, or no results are returned, this is a finding.</check-content></check></Rule></Group>
<!-- DECOY: NOPASSWD (sudo-W01/W05, not W04) -->
<Group id="V-258106"><Rule id="SV-258106r1102061_rule" weight="10.0" severity="medium"><version>RHEL-09-611085</version><title>RHEL 9 must require users to provide a password for privilege escalation.</title><fixtext fixref="F-61771r1045214_fix">Configure RHEL 9 to not allow users to execute privileged actions without authenticating with a password.

Remove any occurrence of "NOPASSWD" found in "/etc/sudoers" file or files in the "/etc/sudoers.d" directory.

$ sudo find /etc/sudoers /etc/sudoers.d -type f -exec sed -i '/NOPASSWD/ s/^/# /g' {} \;</fixtext><check system="C-61847r1102060_chk"><check-content>Verify that "/etc/sudoers" has no occurrences of "NOPASSWD" with the following command:

$ sudo grep -iR 'NOPASSWD' /etc/sudoers /etc/sudoers.d/

If any occurrences of "NOPASSWD" are returned from the command and have not been documented with the information system security officer (ISSO) as an organizationally defined administrative group using multifactor authentication (MFA), this is a finding.</check-content></check></Rule></Group>
</Benchmark>
