<?xml version="1.0" encoding="utf-8"?>
<Benchmark xmlns="http://checklists.nist.gov/xccdf/1.1" id="RHEL8_STIG_V2R4_TEST_FIXTURE">
<title>Trimmed test fixture: real DISA rhel8 RHEL 8 STIG V2R4 (02 Jul 2025) sudo-W04 directive Groups (authenticate / pw_family / timestamp_timeout) plus 1 NOPASSWD (sudo-W01/W05) decoy Group. check-content and fixtext are verbatim; title/description/reference/ident stripped (the parser never reads them).</title>
<!-- authenticate -->
<Group id="V-230272"><Rule id="SV-230272r1101898_rule" weight="10.0" severity="medium"><version>RHEL-08-010381</version><title>RHEL 8 must require users to reauthenticate for privilege escalation.</title><fixtext fixref="F-32916r567563_fix">Remove any occurrence of "!authenticate" found in "/etc/sudoers" file or files in the "/etc/sudoers.d" directory.</fixtext><check system="C-32941r1101897_chk"><check-content>Verify that "/etc/sudoers" has no occurrences of "!authenticate".

Check that the "/etc/sudoers" file has no occurrences of "!authenticate" by running the following command:

$ sudo egrep -iR '!authenticate' /etc/sudoers /etc/sudoers.d/

If any occurrences of "!authenticate" return from the command, this is a finding.</check-content></check></Rule></Group>
<!-- pw_family -->
<Group id="V-237642"><Rule id="SV-237642r991589_rule" weight="10.0" severity="medium"><version>RHEL-08-010383</version><title>RHEL 8 must use the invoking user's password for privilege escalation when using "sudo".</title><fixtext fixref="F-40824r880726_fix">Define the following in the Defaults section of the /etc/sudoers file or a configuration file in the /etc/sudoers.d/ directory:
     Defaults !targetpw
     Defaults !rootpw
     Defaults !runaspw

Remove any configurations that conflict with the above from the following locations: 
     /etc/sudoers
     /etc/sudoers.d/</fixtext><check system="C-40861r880725_chk"><check-content>Verify that the sudoers security policy is configured to use the invoking user's password for privilege escalation.

     $ sudo grep -Eir '(rootpw|targetpw|runaspw)' /etc/sudoers /etc/sudoers.d* | grep -v '#'

     /etc/sudoers:Defaults !targetpw
     /etc/sudoers:Defaults !rootpw
     /etc/sudoers:Defaults !runaspw

If conflicting results are returned, this is a finding.
If "Defaults !targetpw" is not defined, this is a finding.
If "Defaults !rootpw" is not defined, this is a finding.
If "Defaults !runaspw" is not defined, this is a finding.</check-content></check></Rule></Group>
<!-- timestamp_timeout -->
<Group id="V-237643"><Rule id="SV-237643r1050789_rule" weight="10.0" severity="medium"><version>RHEL-08-010384</version><title>RHEL 8 must require re-authentication when using the "sudo" command.</title><fixtext fixref="F-40825r858763_fix">Configure the "sudo" command to require re-authentication.
Edit the /etc/sudoers file:
$ sudo visudo

Add or modify the following line:
Defaults timestamp_timeout=[value]
Note: The "[value]" must be a number that is greater than or equal to "0".

Remove any duplicate or conflicting lines from /etc/sudoers and /etc/sudoers.d/ files.</fixtext><check system="C-40862r861087_chk"><check-content>Verify the operating system requires re-authentication when using the "sudo" command to elevate privileges.

$ sudo grep -ir 'timestamp_timeout' /etc/sudoers /etc/sudoers.d
/etc/sudoers:Defaults timestamp_timeout=0

If conflicting results are returned, this is a finding.

If "timestamp_timeout" is set to a negative number, is commented out, or no results are returned, this is a finding.</check-content></check></Rule></Group>
<!-- DECOY: NOPASSWD (sudo-W01/W05, not W04) -->
<Group id="V-230271"><Rule id="SV-230271r1101896_rule" weight="10.0" severity="medium"><version>RHEL-08-010380</version><title>RHEL 8 must require users to provide a password for privilege escalation.</title><fixtext fixref="F-32915r854025_fix">Remove any occurrence of "NOPASSWD" found in "/etc/sudoers" file or files in the "/etc/sudoers.d" directory.</fixtext><check system="C-32940r1101895_chk"><check-content>Verify that "/etc/sudoers" has no occurrences of "NOPASSWD".

Check that the "/etc/sudoers" file has no occurrences of "NOPASSWD" by running the following command:

$ sudo grep -iR 'NOPASSWD' /etc/sudoers /etc/sudoers.d/

%admin ALL=(ALL) NOPASSWD: ALL

If any occurrences of "NOPASSWD" are returned from the command and have not been documented with the information system security officer (ISSO) as an organizationally defined administrative group utilizing multifactor authentication (MFA), this is a finding.</check-content></check></Rule></Group>
</Benchmark>
