<?xml version="1.0" encoding="utf-8"?>
<Benchmark xmlns="http://checklists.nist.gov/xccdf/1.1" id="RHEL10_STIG_TEST_FIXTURE">
<title>Trimmed test fixture: real DISA rhel10 RHEL 10 STIG V1R1 audit-rule Groups (50) plus 2 non-audit-rule decoys. check-content and fixtext are verbatim; description/references stripped (the parser ignores them). Non-ASCII prose characters transliterated to ASCII (parser-irrelevant).</title>
<Group id="V-281116"><Rule severity="medium"><version>RHEL-10-500300</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "execve" system call.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "execve" system call with the following command:

$ sudo auditctl -l | grep execve
-a always,exit -F arch=b32 -S execve -C uid!=euid -F euid=0 -F key=execpriv
-a always,exit -F arch=b64 -S execve -C uid!=euid -F euid=0 -F key=execpriv
-a always,exit -F arch=b32 -S execve -C gid!=egid -F egid=0 -F key=execpriv
-a always,exit -F arch=b64 -S execve -C gid!=egid -F egid=0 -F key=execpriv

If the command does not return all lines, or the lines are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "execve" system call.

Add or update the following file system rules to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S execve -C uid!=euid -F euid=0 -k execpriv
-a always,exit -F arch=b64 -S execve -C uid!=euid -F euid=0 -k execpriv
-a always,exit -F arch=b32 -S execve -C gid!=egid -F egid=0 -k execpriv
-a always,exit -F arch=b64 -S execve -C gid!=egid -F egid=0 -k execpriv

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281117"><Rule severity="medium"><version>RHEL-10-500310</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "setxattr", "fsetxattr", "lsetxattr", "removexattr", "fremovexattr", and "lremovexattr" system calls.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "setxattr", "fsetxattr", "lsetxattr", "removexattr", "fremovexattr", and "lremovexattr" system calls with the following command:

$ sudo auditctl -l | grep xattr
-a always,exit -F arch=b32 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b64 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid&gt;=1000 -F auid!=unset -k perm_mod

-a always,exit -F arch=b32 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid=0 -k perm_mod
-a always,exit -F arch=b64 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid=0 -k perm_mod

If both the "b32" and "b64" audit rules are not defined for the "setxattr", "fsetxattr", "lsetxattr", "removexattr", "fremovexattr", and "lremovexattr" system calls, or if any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "setxattr", "fsetxattr", "lsetxattr", "removexattr", "fremovexattr", and "lremovexattr" system calls by adding or updating the following lines to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b64 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid&gt;=1000 -F auid!=unset -k perm_mod

-a always,exit -F arch=b32 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid=0 -k perm_mod
-a always,exit -F arch=b64 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid=0 -k perm_mod

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281118"><Rule severity="medium"><version>RHEL-10-500320</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of "umount" system calls.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "umount" command with the following command:

$ sudo auditctl -l | grep /usr/bin/umount
-a always,exit -S all -F path=/usr/bin/umount -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-mount

If the command does not return an audit rule for "umount", or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "umount" command by adding or updating the following rules in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/umount -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-mount

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281119"><Rule severity="medium"><version>RHEL-10-500330</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "chacl" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "chacl" command with the following command:

$ sudo auditctl -l | grep chacl
-a always,exit -S all -F path=/usr/bin/chacl -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "chacl" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/chacl -F perm=x -F auid&gt;=1000 -F auid!=unset -k perm_mod

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281120"><Rule severity="medium"><version>RHEL-10-500340</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "setfacl" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "setfacl" command with the following command:

$ sudo auditctl -l | grep setfacl
-a always,exit -S all -F path=/usr/bin/setfacl -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "setfacl" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/setfacl -F perm=x -F auid&gt;=1000 -F auid!=unset -k perm_mod

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281121"><Rule severity="medium"><version>RHEL-10-500350</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "chcon" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "chcon" command with the following command:

$ sudo auditctl -l | grep chcon
-a always,exit -S all -F path=/usr/bin/chcon -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "chcon" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/chcon -F perm=x -F auid&gt;=1000 -F auid!=unset -k perm_mod

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281122"><Rule severity="medium"><version>RHEL-10-500360</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "semanage" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "semanage" command with the following command:

$ sudo auditctl -l | grep semanage
-a always,exit -S all -F path=/usr/sbin/semanage -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "semanage" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/semanage -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281123"><Rule severity="medium"><version>RHEL-10-500370</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "setfiles" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "setfiles" command with the following command:

$ sudo auditctl -l | grep setfiles
-a always,exit -S all -F path=/usr/sbin/setfiles -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "setfiles" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/setfiles -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281124"><Rule severity="medium"><version>RHEL-10-500380</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "setsebool" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "setsebool" command with the following command:

$ sudo auditctl -l | grep setsebool
-a always,exit -S all -F path=/usr/sbin/setsebool -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful use of the "setsebool " command by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/setsebool -F perm=x -F auid&gt;=1000 -F auid!=unset -F key=privileged

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281125"><Rule severity="medium"><version>RHEL-10-500390</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "truncate", "ftruncate", "creat", "open", "openat", and "open_by_handle_at" system calls.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit successful/unsuccessful attempts to use the "truncate", "ftruncate", "creat", "open", "openat", and "open_by_handle_at" system calls with the following command:

$ sudo auditctl -l | grep 'open\|truncate\|creat'
-a always,exit -F arch=b32 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EPERM -F auid&gt;=1000 -F auid!=unset -k perm_access
-a always,exit -F arch=b64 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EPERM -F auid&gt;=1000 -F auid!=unset -k perm_access

-a always,exit -F arch=b32 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EACCES -F auid&gt;=1000 -F auid!=unset -k perm_access
-a always,exit -F arch=b64 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EACCES -F auid&gt;=1000 -F auid!=unset -k perm_access

If the output does not produce rules containing "-F exit=-EPERM", this is a finding.

If the output does not produce rules containing "-F exit=-EACCES", this is a finding.

If the command does not return an audit rule for "truncate", "ftruncate", "creat", "open", "openat", and "open_by_handle_at", or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful use of the "truncate", "ftruncate", "creat", "open", "openat", and "open_by_handle_at" system calls by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F arch=b32 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EPERM -F auid&gt;=1000 -F auid!=unset -k perm_access
-a always,exit -F arch=b64 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EPERM -F auid&gt;=1000 -F auid!=unset -k perm_access

-a always,exit -F arch=b32 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EACCES -F auid&gt;=1000 -F auid!=unset -k perm_access
-a always,exit -F arch=b64 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EACCES -F auid&gt;=1000 -F auid!=unset -k perm_access

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281126"><Rule severity="medium"><version>RHEL-10-500400</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "delete_module" system call.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "delete_module" system call with the following command:

$ sudo auditctl -l | grep delete_module
-a always,exit -F arch=b32 -S delete_module -F auid&gt;=1000 -F auid!=-1 -F key=module_chng
-a always,exit -F arch=b64 -S delete_module -F auid&gt;=1000 -F auid!=-1 -F key=module_chng

If both the "b32" and "b64" audit rules are not defined for the "delete_module" system call, or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful use of the "delete_module" system call by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F arch=b32 -S delete_module -F auid&gt;=1000 -F auid!=unset -k module_chng
-a always,exit -F arch=b64 -S delete_module -F auid&gt;=1000 -F auid!=unset -k module_chng

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281127"><Rule severity="medium"><version>RHEL-10-500410</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "init_module" and "finit_module" system calls.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "init_module" and "finit_module" syscalls with the following command:

$ sudo auditctl -l | grep init_module
-a always,exit -F arch=b32 -S init_module,finit_module -F auid&gt;=1000 -F auid!=unset -k module_chng
-a always,exit -F arch=b64 -S init_module,finit_module -F auid&gt;=1000 -F auid!=unset -k module_chng

If both the "b32" and "b64" audit rules are not defined for the "delete_module" syscall, or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful use of the "init_module" and "finit_module" system calls by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F arch=b32 -S init_module,finit_module -F auid&gt;=1000 -F auid!=unset -k module_chng
-a always,exit -F arch=b64 -S init_module,finit_module -F auid&gt;=1000 -F auid!=unset -k module_chng

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281128"><Rule severity="medium"><version>RHEL-10-500420</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "chage" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "chage" command with the following command:

$ sudo auditctl -l | grep chage
-a always,exit -S all -F path=/usr/bin/chage -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-chage

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "chage" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/chage -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-chage

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281129"><Rule severity="medium"><version>RHEL-10-500430</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "chsh" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "chsh" command with the following command:

$ sudo auditctl -l | grep chsh
-a always,exit -S all -F path=/usr/bin/chsh -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=priv_cmd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "chsh" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/chsh -F perm=x -F auid&gt;=1000 -F auid!=unset -k priv_cmd

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281130"><Rule severity="medium"><version>RHEL-10-500440</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "crontab" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "crontab" command with the following command:

$ sudo auditctl -l | grep crontab
-a always,exit -S all -F path=/usr/bin/crontab -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-crontab

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "crontab" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/crontab -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-crontab

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281131"><Rule severity="medium"><version>RHEL-10-500450</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "gpasswd" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "gpasswd" command with the following command:

$ sudo auditctl -l | grep gpasswd
-a always,exit -S all -F path=/usr/bin/gpasswd -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-gpasswd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "gpasswd" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/gpasswd -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-gpasswd

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281132"><Rule severity="medium"><version>RHEL-10-500460</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "kmod" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "kmod" command with the following command:

$ sudo auditctl -l | grep kmod
-a always,exit -S all -F path=/usr/bin/kmod -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=modules

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "kmod" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/kmod -F perm=x -F auid&gt;=1000 -F auid!=unset -k modules

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281133"><Rule severity="medium"><version>RHEL-10-500470</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "newgrp" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "newgrp" command with the following command:

$ sudo auditctl -l | grep newgrp
-a always,exit -S all -F path=/usr/bin/newgrp -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=priv_cmd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "newgrp" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/newgrp -F perm=x -F auid&gt;=1000 -F auid!=unset -k priv_cmd

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281134"><Rule severity="medium"><version>RHEL-10-500480</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "pam_timestamp_check" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "pam_timestamp_check" command with the following command:

$ sudo auditctl -l | grep timestamp
-a always,exit -S all -F path=/usr/sbin/pam_timestamp_check -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-pam_timestamp_check

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "pam_timestamp_check" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/pam_timestamp_check -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-pam_timestamp_check

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281135"><Rule severity="medium"><version>RHEL-10-500490</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "passwd" command.</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/passwd" with the following command:

$ sudo auditctl -l | egrep '(/usr/bin/passwd)'
-a always,exit -S all -F path=/usr/bin/passwd -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-passwd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "passwd" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/passwd -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-passwd

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281136"><Rule severity="medium"><version>RHEL-10-500500</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "postdrop" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "postdrop" command with the following command:

$ sudo auditctl -l | grep postdrop
-a always,exit -S all -F path=/usr/sbin/postdrop -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "postdrop" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/postdrop -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281137"><Rule severity="medium"><version>RHEL-10-500510</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "postqueue" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "postqueue" command with the following command:

$ sudo auditctl -l | grep postqueue
-a always,exit -S all -F path=/usr/sbin/postqueue -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "postqueue" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/postqueue -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281138"><Rule severity="medium"><version>RHEL-10-500520</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the ssh-agent command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "ssh-agent" command with the following command:

$ sudo auditctl -l | grep ssh-agent
-a always,exit -S all -F path=/usr/bin/ssh-agent -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-ssh

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "ssh-agent" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/ssh-agent -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-ssh

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281139"><Rule severity="medium"><version>RHEL-10-500530</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "ssh-keysign" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "ssh-keysign" command with the following command:

$ sudo auditctl -l | grep ssh-keysign
-a always,exit -S all -F path=/usr/libexec/openssh/ssh-keysign -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-ssh

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "ssh-keysign" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/libexec/openssh/ssh-keysign -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-ssh

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281140"><Rule severity="medium"><version>RHEL-10-500540</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "su" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "su" command with the following command:

$ sudo auditctl -l | grep '/usr/bin/su\b'
-a always,exit -S all -F path=/usr/bin/su -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-priv_change

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "su" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/su -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-priv_change

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281141"><Rule severity="medium"><version>RHEL-10-500550</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "sudo" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "sudo" command with the following command:

$ sudo auditctl -l | grep '/usr/bin/sudo\b'
-a always,exit -S all -F path=/usr/bin/sudo -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=priv_cmd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "sudo" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/sudo -F perm=x -F auid&gt;=1000 -F auid!=unset -k priv_cmd

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281142"><Rule severity="medium"><version>RHEL-10-500560</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "sudoedit" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "sudoedit" command with the following command:

$ sudo auditctl -l | grep /usr/bin/sudoedit
-a always,exit -S all -F path=/usr/bin/sudoedit -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=priv_cmd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "sudoedit" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/sudoedit -F perm=x -F auid&gt;=1000 -F auid!=unset -k priv_cmd

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281143"><Rule severity="medium"><version>RHEL-10-500570</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "unix_chkpwd" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "unix_chkpwd" command with the following command:

$ sudo auditctl -l | grep unix_chkpwd
-a always,exit -S all -F path=/usr/sbin/unix_chkpwd -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "unix_chkpwd" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/unix_chkpwd -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281144"><Rule severity="medium"><version>RHEL-10-500580</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "unix_update" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "unix_update" command with the following command:

$ sudo auditctl -l | grep unix_update
-a always,exit -S all -F path=/usr/sbin/unix_update -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "unix_update" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/unix_update -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281145"><Rule severity="medium"><version>RHEL-10-500590</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "userhelper" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "userhelper" command with the following command:

$ sudo auditctl -l | grep userhelper
-a always,exit -S all -F path=/usr/sbin/userhelper -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "userhelper" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/userhelper -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281146"><Rule severity="medium"><version>RHEL-10-500600</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "usermod" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "usermod" command with the following command:

$ sudo auditctl -l | grep usermod
-a always,exit -S all -F path=/usr/sbin/usermod -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-usermod

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "usermod" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/usermod -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-usermod

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281147"><Rule severity="medium"><version>RHEL-10-500610</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "mount" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "mount" command with the following command:

$ sudo auditctl -l | grep mount
-a always,exit -F arch=b32 -S mount -F auid&gt;=1000 -F auid!=unset -k export
-a always,exit -F arch=b64 -S mount -F auid&gt;=1000 -F auid!=unset -k export

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "mount" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S mount -F auid&gt;=1000 -F auid!=unset -k export
-a always,exit -F arch=b64 -S mount -F auid&gt;=1000 -F auid!=unset -k export

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281148"><Rule severity="medium"><version>RHEL-10-500620</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "init" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "init" command with the following command:

$ sudo auditctl -l | grep /usr/sbin/init
-a always,exit -S all -F path=/usr/sbin/init -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-init

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful uses of the "init" command by adding or updating the following rule in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/init -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-init

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281149"><Rule severity="medium"><version>RHEL-10-500630</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "poweroff" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "poweroff" command with the following command:

$ sudo auditctl -l | grep poweroff
-a always,exit -S all -F path=/usr/sbin/poweroff -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-poweroff

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful uses of the "poweroff" command by adding or updating the following rule in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/poweroff -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-poweroff

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281150"><Rule severity="medium"><version>RHEL-10-500640</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "reboot" command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "reboot" command with the following command:

$ sudo auditctl -l | grep reboot
-a always,exit -S all -F path=/usr/sbin/reboot -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-reboot

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful uses of the "reboot" command by adding or updating the following rule in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/reboot -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-reboot

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281151"><Rule severity="medium"><version>RHEL-10-500650</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the shutdown command.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "shutdown" command with the following command:

$ sudo cat /etc/audit/rules.d/* | grep shutdown
-a always,exit -S all -F path=/usr/sbin/shutdown -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-shutdown

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful uses of the "shutdown" command by adding or updating the following rule in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/shutdown -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-shutdown

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281152"><Rule severity="medium"><version>RHEL-10-500660</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "umount" system call.</title><check system="C-x"><check-content>Verify RHEL 10 generates an audit record for all uses of the "umount" system call with the following command:

$ sudo auditctl -l | grep b32 | grep 'umount\b'
-a always,exit -F arch=b32 -S umount -F auid&gt;=1000 -F auid!=-1 -F key=privileged-umount

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful uses of the "umount" system call by adding or updating the following rules in "/etc/audit/audit.rules" and adding the following rules to "/etc/audit/rules.d/perm_mod.rules" or updating the existing rules in files in the "/etc/audit/rules.d/" directory:

-a always,exit -F arch=b32 -S umount -F auid&gt;=1000 -F auid!=unset -k privileged-umount

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281153"><Rule severity="medium"><version>RHEL-10-500670</version><title>RHEL 10 must generate audit records for successful and unsuccessful uses of the "umount2" system call.</title><check system="C-x"><check-content>Verify RHEL 10 generates an audit record for all uses of the "umount2" system call with the following command:

$ sudo auditctl -l | grep umount2
-a always,exit -F arch=b64 -S umount2 -F auid&gt;=1000 -F auid!=-1 -F key=privileged-umount
-a always,exit -F arch=b32 -S umount2 -F auid&gt;=1000 -F auid!=-1 -F key=privileged-umount

If no line is returned, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful uses of the "umount2" system call by adding or updating the following rules in a file in "/etc/audit/rules.d":

-a always,exit -F arch=b32 -S umount2 -F auid&gt;=1000 -F auid!=unset -k privileged-umount
-a always,exit -F arch=b64 -S umount2 -F auid&gt;=1000 -F auid!=unset -k privileged-umount

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281154"><Rule severity="medium"><version>RHEL-10-500680</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers".</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers" with the following command:

$ sudo auditctl -l | grep '/etc/sudoers[^.]'
-a always,exit -F arch=b32 -F path=/etc/sudoers -F perm=wa -F key=logins
-a always,exit -F arch=b64 -F path=/etc/sudoers -F perm=wa -F key=logins

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/etc/sudoers -F perm=wa -F key=logins
-a always,exit -F arch=b64 -F path=/etc/sudoers -F perm=wa -F key=logins

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281155"><Rule severity="medium"><version>RHEL-10-500690</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect the "/etc/sudoers.d/" directory.</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers.d/" with the following command:

$ sudo auditctl -l | grep /etc/sudoers.d
-a always,exit -F arch=b32 -F path=/etc/sudoers.d/ -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/sudoers.d/ -F perm=wa -F key=identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers.d/".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/etc/sudoers.d/ -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/sudoers.d/ -F perm=wa -F key=identity

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281156"><Rule severity="medium"><version>RHEL-10-500700</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/group".</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/group" with the following command:

$ sudo auditctl -l | egrep '(/etc/group)'
-a always,exit -F arch=b32 -F path=/etc/group -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/group -F perm=wa -F key=identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/group".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/etc/group -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/group -F perm=wa -F key=identity

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281157"><Rule severity="medium"><version>RHEL-10-500710</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/gshadow".</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/gshadow" with the following command:

$ sudo auditctl -l | egrep '(/etc/gshadow)'
-a always,exit -F arch=b32 -F path=/etc/gshadow -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/gshadow -F perm=wa -F key=identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/gshadow".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/etc/gshadow -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/gshadow -F perm=wa -F key=identity

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281158"><Rule severity="medium"><version>RHEL-10-500720</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/opasswd".</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/security/opasswd" with the following command:

$ sudo auditctl -l | egrep '(/etc/security/opasswd)'
-a always,exit -F arch=b32 -F path=/etc/security/opasswd -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/security/opasswd -F perm=wa -F key=identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/security/opasswd".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/etc/security/opasswd -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=(/etc/security/opasswd -F perm=wa -F key=identity

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281159"><Rule severity="medium"><version>RHEL-10-500730</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/passwd".</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/passwd" with the following command:

$ sudo auditctl -l | egrep '(/etc/passwd)'
-a always,exit -F arch=b32 -F path=/etc/passwd -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/passwd -F perm=wa -F key=identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/passwd".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/etc/passwd -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/passwd -F perm=wa -F key=identity

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281160"><Rule severity="medium"><version>RHEL-10-500740</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/shadow".</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/shadow" with the following command:

$ sudo auditctl -l | egrep '(/etc/shadow)'
-a always,exit -F arch=b32 -F path=/etc/shadow -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/shadow -F perm=wa -F key=identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/shadow".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/etc/shadow -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/etc/shadow -F perm=wa -F key=identity

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281161"><Rule severity="medium"><version>RHEL-10-500750</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/faillock".</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/faillock" with the following command:

$ sudo auditctl -l | grep /var/log/faillock
-a always,exit -F arch=b32 -F path=/var/log/faillock -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/var/log/faillock -F perm=wa -F key=identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/faillock".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/var/log/faillock -F perm=wa -F key=identity
-a always,exit -F arch=b64 -F path=/var/log/faillock -F perm=wa -F key=identity

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281162"><Rule severity="medium"><version>RHEL-10-500760</version><title>RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/lastlog".</title><check system="C-x"><check-content>Verify RHEL 10 generates audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/lastlog" with the following command:

$ sudo auditctl -l | grep /var/log/lastlog
-a always,exit -F arch=b32 -F path=/var/log/lastlog -F perm=wa -F key=logins
-a always,exit -F arch=b64 -F path=/var/log/lastlog -F perm=wa -F key=logins

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/lastlog".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -F path=/var/log/lastlog -F perm=wa -F key=logins
-a always,exit -F arch=b64 -F path=/var/log/lastlog -F perm=wa -F key=logins

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281163"><Rule severity="medium"><version>RHEL-10-500780</version><title>RHEL 10 must generate audit records for all uses of the "chmod", "fchmod", "fchmodat", and "fchmodat2" syscalls.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "chmod", "fchmod", "fchmodat", and "fchmodat2" syscalls with the following command:

$ sudo auditctl -l | grep chmod
-a always,exit -F arch=b32 -S chmod,fchmod,fchmodat,fchmodat2 -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b64 -S chmod,fchmod,fchmodat,fchmodat2 -F auid&gt;=1000 -F auid!=unset -k perm_mod

If both the "b32" and "b64" audit rules are not defined for the "chmod", "fchmod", "fchmodat", and "fchmodat2" syscalls, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful and unsuccessful attempts to use the "chmod", "fchmod", "fchmodat", and "fchmodat2" syscalls.

Add or update the following rules in "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S chmod,fchmod,fchmodat,fchmodat2 -F auid&gt;=1000 -F auid!=unset -k perm_mod

-a always,exit -F arch=b64 -S chmod,fchmod,fchmodat,fchmodat2 -F auid&gt;=1000 -F auid!=unset -k perm_mod

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281164"><Rule severity="medium"><version>RHEL-10-500790</version><title>RHEL 10 must generate audit records for all uses of the "chown", "fchown", "fchownat", and "lchown" syscalls.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit the execution of the "chown", "fchown", "fchownat", and "lchown" syscalls with the following command:

$ sudo auditctl -l | grep chown
-a always,exit -F arch=b32 -S chown,fchown,fchownat,lchown -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b64 -S chown,fchown,fchownat,lchown -F auid&gt;=1000 -F auid!=unset -k perm_mod

If both the "b32" and "b64" audit rules are not defined for the "chown", "fchown", "fchownat", and "lchown" syscalls, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate audit records upon successful/unsuccessful attempts to use the "chown", "fchown", "fchownat", and "lchown"" syscalls.

Add or update the following rules in "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S chown,fchown,fchownat,lchown -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b64 -S chown,fchown,fchownat,lchown -F auid&gt;=1000 -F auid!=unset -k perm_mod

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-281165"><Rule severity="medium"><version>RHEL-10-500810</version><title>RHEL 10 must generate audit records for all uses of the "rename", "unlink", "rmdir", "renameat", "renameat2", and "unlinkat" system calls.</title><check system="C-x"><check-content>Verify RHEL 10 is configured to audit successful/unsuccessful attempts to use the "rename", "unlink", "rmdir", "renameat", "renameat2", and "unlinkat" system calls with the following command:

$ sudo auditctl -l | grep 'rename\|unlink\|rmdir'
-a always,exit -F arch=b32 -S rename,unlink,rmdir,renameat,renameat2,unlinkat -F auid&gt;=1000 -F auid!=unset -k delete
-a always,exit -F arch=b64 -S rename,unlink,rmdir,renameat,renameat2,unlinkat -F auid&gt;=1000 -F auid!=unset -k delete

If the command does not return an audit rule for "rename", "unlink", "rmdir", "renameat", and "unlinkat", or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to generate an audit event for any successful/unsuccessful use of the "rename", "unlink", "rmdir", "renameat", "renameat2", and "unlinkat" system calls by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F arch=b32 -S rename,unlink,rmdir,renameat,renameat2,unlinkat -F auid&gt;=1000 -F auid!=unset -k delete
-a always,exit -F arch=b64 -S rename,unlink,rmdir,renameat,renameat2,unlinkat -F auid&gt;=1000 -F auid!=unset -k delete

Restart the audit daemon with the following command for the changes to take effect:

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-280994"><Rule severity="medium"><version>RHEL-10-200661</version><title>RHEL 10 must enable the audit service.</title><check system="C-x"><check-content>Verify RHEL 10 enables the audit service to produce audit records with the following command:

$ systemctl status auditd.service
o auditd.service - Security Audit Logging Service
     Loaded: loaded (/usr/lib/systemd/system/auditd.service; enabled; preset: enabled)
     Active: active (running) since Wed 2025-10-01 14:00:21 EDT; 1 week 6 days ago

If the audit service is not "active" and "running", this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to enable the auditd service with the following command:

$ sudo systemctl enable --now auditd</fixtext></Rule></Group>
<Group id="V-281050"><Rule severity="medium"><version>RHEL-10-400165</version><title>RHEL 10 must enforce group ownership of audit logs by "root" or by a restricted logging group to prevent unauthorized read access.</title><check system="C-x"><check-content>Verify RHEL 10 audit logs are group-owned by "root" or a restricted logging group.

Determine where the audit logs are stored with the following command:

$ sudo grep -iw log_file /etc/audit/auditd.conf
log_file = /var/log/audit/audit.log

Using the location of the audit log file, determine if the audit log is group-owned by "root" using the following command:

$ sudo stat -c "%G %n" /var/log/audit/audit.log
root /var/log/audit/audit.log

If the audit log is not group-owned by "root" or the configured alternative logging group, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 10 to prevent unauthorized read access by ensuring that audit logs are group-owned by root or by a restricted logging group.

Change the group of the directory of "/var/log/audit" to be owned by a correct group.

Identify the group that is configured to own audit logs:

$ sudo grep -P '^[ ]*log_group[ ]+=.*$' /etc/audit/auditd.conf

Change the ownership to that group:

$ sudo chgrp ${GROUP} /var/log/audit</fixtext></Rule></Group>
</Benchmark>
