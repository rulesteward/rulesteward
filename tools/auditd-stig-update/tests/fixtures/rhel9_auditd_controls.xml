<?xml version="1.0" encoding="utf-8"?>
<Benchmark xmlns="http://checklists.nist.gov/xccdf/1.1" id="RHEL9_STIG_TEST_FIXTURE">
<title>Trimmed test fixture: real DISA rhel9 RHEL 9 STIG V2R7 audit-rule Groups (51) plus 2 non-audit-rule decoys. check-content and fixtext are verbatim; description/references stripped (the parser ignores them). Non-ASCII prose characters transliterated to ASCII (parser-irrelevant).</title>
<Group id="V-258176"><Rule severity="medium"><version>RHEL-09-654010</version><title>RHEL 9 must audit uses of the "execve" system call.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit the execution of the "execve" system call with the following command:

$ sudo auditctl -l | grep execve

-a always,exit -F arch=b32 -S execve -C uid!=euid -F euid=0 -k execpriv
-a always,exit -F arch=b64 -S execve -C uid!=euid -F euid=0 -k execpriv
-a always,exit -F arch=b32 -S execve -C gid!=egid -F egid=0 -k execpriv
-a always,exit -F arch=b64 -S execve -C gid!=egid -F egid=0 -k execpriv

If the command does not return all lines, or the lines are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to audit the execution of the "execve" system call.

Add or update the following file system rules to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S execve -C uid!=euid -F euid=0 -k execpriv 
-a always,exit -F arch=b64 -S execve -C uid!=euid -F euid=0 -k execpriv
-a always,exit -F arch=b32 -S execve -C gid!=egid -F egid=0 -k execpriv 
-a always,exit -F arch=b64 -S execve -C gid!=egid -F egid=0 -k execpriv 

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258177"><Rule severity="medium"><version>RHEL-09-654015</version><title>RHEL 9 must audit all uses of the chmod, fchmod, and fchmodat system calls.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit the execution of the "chmod", "fchmod", and "fchmodat" system calls with the following command:

$ sudo auditctl -l | grep chmod

-a always,exit -F arch=b32 -S chmod,fchmod,fchmodat -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod
-a always,exit -F arch=b64 -S chmod,fchmod,fchmodat -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod

If both the "b32" and "b64" audit rules are not defined for the "chmod", "fchmod", and "fchmodat" system calls, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "chmod", "fchmod", and "fchmodat" syscalls.

Add or update the following rules in "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S chmod,fchmod,fchmodat -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b64 -S chmod,fchmod,fchmodat -F auid&gt;=1000 -F auid!=unset -k perm_mod

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258178"><Rule severity="medium"><version>RHEL-09-654020</version><title>RHEL 9 must audit all uses of the chown, fchown, fchownat, and lchown system calls.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit the execution of the  "chown", "fchown", "fchownat", and "lchown" system calls with the following command:

$ sudo auditctl -l | grep chown

-a always,exit -F arch=b32 -S lchown,fchown,chown,fchownat -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod
-a always,exit -F arch=b64 -S chown,fchown,lchown,fchownat -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod

If both the "b32" and "b64" audit rules are not defined for the "chown", "fchown", "fchownat", and "lchown" system calls, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "chown", "fchown", "fchownat", and "lchown"" system calls.

Add or update the following rules in "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S chown,fchown,fchownat,lchown -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b64 -S chown,fchown,fchownat,lchown -F auid&gt;=1000 -F auid!=unset -k perm_mod

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258179"><Rule severity="medium"><version>RHEL-09-654025</version><title>RHEL 9 must audit all uses of the setxattr, fsetxattr, lsetxattr, removexattr, fremovexattr, and lremovexattr system calls.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit the execution of the "setxattr", "fsetxattr", "lsetxattr", "removexattr", "fremovexattr", and "lremovexattr" system calls with the following command:

$ sudo auditctl -l | grep xattr

-a always,exit -F arch=b32 -S setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremovexattr -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod
-a always,exit -F arch=b64 -S setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremovexattr -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod
-a always,exit -F arch=b32 -S setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremovexattr -F auid=0 -F key=perm_mod
-a always,exit -F arch=b64 -S setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremovexattr -F auid=0 -F key=perm_mod

If both the "b32" and "b64" audit rules are not defined for the "setxattr", "fsetxattr", "lsetxattr", "removexattr", "fremovexattr", and "lremovexattr" system calls, or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to audit the execution of the "setxattr", "fsetxattr", "lsetxattr", "removexattr", "fremovexattr", and "lremovexattr" system calls by adding or updating the following lines to "/etc/audit/rules.d/audit.rules":

-a always,exit -F arch=b32 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b64 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid&gt;=1000 -F auid!=unset -k perm_mod
-a always,exit -F arch=b32 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid=0 -k perm_mod
-a always,exit -F arch=b64 -S setxattr,fsetxattr,lsetxattr,removexattr,fremovexattr,lremovexattr -F auid=0 -k perm_mod

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258180"><Rule severity="medium"><version>RHEL-09-654030</version><title>RHEL 9 must audit all uses of umount system calls.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "umount" command with the following command:

$ sudo auditctl -l | grep /usr/bin/umount

-a always,exit -S all -F path=/usr/bin/umount -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-mount

If the command does not return an audit rule for "umount" or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "umount" command by adding or updating the following rules in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/umount -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-mount

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258181"><Rule severity="medium"><version>RHEL-09-654035</version><title>RHEL 9 must audit all uses of the chacl command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "chacl" command with the following command:

$ sudo auditctl -l | grep chacl

-a always,exit -S all -F path=/usr/bin/chacl -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "chacl" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/chacl -F perm=x -F auid&gt;=1000 -F auid!=unset -k perm_mod

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258182"><Rule severity="medium"><version>RHEL-09-654040</version><title>RHEL 9 must audit all uses of the setfacl command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "setfacl" command with the following command:

$ sudo auditctl -l | grep setfacl

-a always,exit -S all -F path=/usr/bin/setfacl -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "setfacl" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/setfacl -F perm=x -F auid&gt;=1000 -F auid!=unset -k perm_mod

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258183"><Rule severity="medium"><version>RHEL-09-654045</version><title>RHEL 9 must audit all uses of the chcon command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "chcon" command with the following command:

$ sudo auditctl -l | grep chcon

-a always,exit -S all -F path=/usr/bin/chcon -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=perm_mod

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "chcon" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/chcon -F perm=x -F auid&gt;=1000 -F auid!=unset -k perm_mod

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258184"><Rule severity="medium"><version>RHEL-09-654050</version><title>RHEL 9 must audit all uses of the semanage command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "semanage" command with the following command:

$ sudo auditctl -l | grep semanage

-a always,exit -S all -F path=/usr/sbin/semanage -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "semanage" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/semanage -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258185"><Rule severity="medium"><version>RHEL-09-654055</version><title>RHEL 9 must audit all uses of the setfiles command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "setfiles" command with the following command:

$ sudo auditctl -l | grep setfiles

-a always,exit -S all -F path=/usr/sbin/setfiles -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "setfiles" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/setfiles -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258186"><Rule severity="medium"><version>RHEL-09-654060</version><title>RHEL 9 must audit all uses of the setsebool command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "setsebool" command with the following command:

$ sudo auditctl -l | grep setsebool

-a always,exit -S all -F path=/usr/sbin/setsebool -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate an audit event for any successful/unsuccessful use of the "setsebool " command by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/setsebool -F perm=x -F auid&gt;=1000 -F auid!=unset -F key=privileged 

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258187"><Rule severity="medium"><version>RHEL-09-654065</version><title>RHEL 9 must audit all uses of the rename, unlink, rmdir, renameat, and unlinkat system calls.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit successful/unsuccessful attempts to use the "rename", "unlink", "rmdir", "renameat", and "unlinkat" system calls with the following command:

$ sudo auditctl -l | grep 'rename\|unlink\|rmdir'

-a always,exit -F arch=b32 -S unlink,rename,rmdir,unlinkat,renameat -F auid&gt;=1000 -F auid!=-1 -F key=delete
-a always,exit -F arch=b64 -S rename,rmdir,unlink,unlinkat,renameat -F auid&gt;=1000 -F auid!=-1 -F key=delete

If the command does not return an audit rule for "rename", "unlink", "rmdir", "renameat", and "unlinkat" or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate an audit event for any successful/unsuccessful use of the "rename", "unlink", "rmdir", "renameat", and "unlinkat" system calls by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F arch=b32 -S rename,unlink,rmdir,renameat,unlinkat -F auid&gt;=1000 -F auid!=unset -k delete
-a always,exit -F arch=b64 -S rename,unlink,rmdir,renameat,unlinkat -F auid&gt;=1000 -F auid!=unset -k delete

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258188"><Rule severity="medium"><version>RHEL-09-654070</version><title>RHEL 9 must audit all uses of the truncate, ftruncate, creat, open, openat, and open_by_handle_at system calls.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit successful/unsuccessful attempts to use the "truncate", "ftruncate", "creat", "open", "openat", and "open_by_handle_at" system calls with the following command:

$ sudo auditctl -l | grep 'open\b\|openat\|open_by_handle_at\|truncate\|creat'

-a always,exit -F arch=b32 -S open,creat,truncate,ftruncate,openat,open_by_handle_at -F exit=-EPERM -F auid&gt;=1000 -F auid!=-1 -F key=perm_access
-a always,exit -F arch=b64 -S open,truncate,ftruncate,creat,openat,open_by_handle_at -F exit=-EPERM -F auid&gt;=1000 -F auid!=-1 -F key=perm_access
-a always,exit -F arch=b32 -S open,creat,truncate,ftruncate,openat,open_by_handle_at -F exit=-EACCES -F auid&gt;=1000 -F auid!=-1 -F key=perm_access
-a always,exit -F arch=b64 -S open,truncate,ftruncate,creat,openat,open_by_handle_at -F exit=-EACCES -F auid&gt;=1000 -F auid!=-1 -F key=perm_access

If the output does not produce rules containing "-F exit=-EPERM", this is a finding.

If the output does not produce rules containing "-F exit=-EACCES", this is a finding.

If the command does not return an audit rule for "truncate", "ftruncate", "creat", "open", "openat", and "open_by_handle_at" or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate an audit event for any successful/unsuccessful use of the "truncate", "ftruncate", "creat", "open", "openat", and "open_by_handle_at" system calls by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F arch=b32 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EPERM -F auid&gt;=1000 -F auid!=unset -k perm_access
-a always,exit -F arch=b64 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EPERM -F auid&gt;=1000 -F auid!=unset -k perm_access

-a always,exit -F arch=b32 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EACCES -F auid&gt;=1000 -F auid!=unset -k perm_access
-a always,exit -F arch=b64 -S truncate,ftruncate,creat,open,openat,open_by_handle_at -F exit=-EACCES -F auid&gt;=1000 -F auid!=unset -k perm_access

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258189"><Rule severity="medium"><version>RHEL-09-654075</version><title>RHEL 9 must audit all uses of the delete_module system call.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit the execution of the "delete_module" system call with the following command:

$ sudo auditctl -l | grep delete_module

-a always,exit -F arch=b32 -S delete_module -F auid&gt;=1000 -F auid!=-1 -F key=module_chng
-a always,exit -F arch=b64 -S delete_module -F auid&gt;=1000 -F auid!=-1 -F key=module_chng

If both the "b32" and "b64" audit rules are not defined for the "delete_module" system call, or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate an audit event for any successful/unsuccessful use of the "delete_module" system call by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F arch=b32 -S delete_module -F auid&gt;=1000 -F auid!=unset -k module_chng
-a always,exit -F arch=b64 -S delete_module -F auid&gt;=1000 -F auid!=unset -k module_chng

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258190"><Rule severity="medium"><version>RHEL-09-654080</version><title>RHEL 9 must audit all uses of the init_module and finit_module system calls.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit the execution of the "init_module" and "finit_module" system calls with the following command:

$ sudo auditctl -l | grep init_module

-a always,exit -F arch=b32 -S init_module,finit_module -F auid&gt;=1000 -F auid!=-1 -F key=module_chng
-a always,exit -F arch=b64 -S init_module,finit_module -F auid&gt;=1000 -F auid!=-1 -F key=module_chng

If both the "b32" and "b64" audit rules are not defined for the "init_module" system call, or any of the lines returned are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate an audit event for any successful/unsuccessful use of the "init_module" and "finit_module" system calls by adding or updating the following rules in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F arch=b32 -S init_module,finit_module -F auid&gt;=1000 -F auid!=unset -k module_chng
-a always,exit -F arch=b64 -S init_module,finit_module -F auid&gt;=1000 -F auid!=unset -k module_chng

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258191"><Rule severity="medium"><version>RHEL-09-654085</version><title>RHEL 9 must audit all uses of the chage command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "chage" command with the following command:

$ sudo auditctl -l | grep chage

-a always,exit -S all -F path=/usr/bin/chage -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-chage

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "chage" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/chage -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-chage

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258192"><Rule severity="medium"><version>RHEL-09-654090</version><title>RHEL 9 must audit all uses of the chsh command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "chsh" command with the following command:

$ sudo auditctl -l | grep chsh

-a always,exit -S all -F path=/usr/bin/chsh -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=priv_cmd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "chsh" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/chsh -F perm=x -F auid&gt;=1000 -F auid!=unset -k priv_cmd

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258193"><Rule severity="medium"><version>RHEL-09-654095</version><title>RHEL 9 must audit all uses of the crontab command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "crontab" command with the following command:

$ sudo auditctl -l | grep crontab

-a always,exit -S all -F path=/usr/bin/crontab -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-crontab

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "crontab" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/crontab -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-crontab

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258194"><Rule severity="medium"><version>RHEL-09-654100</version><title>RHEL 9 must audit all uses of the gpasswd command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "gpasswd" command with the following command:

$ sudo auditctl -l | grep gpasswd

-a always,exit -S all -F path=/usr/bin/gpasswd -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-gpasswd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "gpasswd" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/gpasswd -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-gpasswd

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258195"><Rule severity="medium"><version>RHEL-09-654105</version><title>RHEL 9 must audit all uses of the kmod command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "kmod" command with the following command:

$ sudo auditctl -l | grep kmod

-a always,exit -S all -F path=/usr/bin/kmod -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=modules

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "kmod" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/kmod -F perm=x -F auid&gt;=1000 -F auid!=unset -k modules

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258196"><Rule severity="medium"><version>RHEL-09-654110</version><title>RHEL 9 must audit all uses of the newgrp command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "newgrp" command with the following command:

$ sudo auditctl -l | grep newgrp

-a always,exit -S all -F path=/usr/bin/newgrp -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=priv_cmd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "newgrp" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/newgrp -F perm=x -F auid&gt;=1000 -F auid!=unset -k priv_cmd

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258197"><Rule severity="medium"><version>RHEL-09-654115</version><title>RHEL 9 must audit all uses of the pam_timestamp_check command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "pam_timestamp_check" command with the following command:

$ sudo auditctl -l | grep timestamp

-a always,exit -S all -F path=/usr/sbin/pam_timestamp_check -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-pam_timestamp_check

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "pam_timestamp_check" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/pam_timestamp_check -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-pam_timestamp_check

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258198"><Rule severity="medium"><version>RHEL-09-654120</version><title>RHEL 9 must audit all uses of the passwd command.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/gshadow" with the following command:

$ sudo auditctl -l | egrep '(/usr/bin/passwd)' 

-a always,exit -S all -F path=/usr/bin/passwd -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-passwd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "passwd" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/passwd -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-passwd

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258199"><Rule severity="medium"><version>RHEL-09-654125</version><title>RHEL 9 must audit all uses of the postdrop command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "postdrop" command with the following command:

$ sudo auditctl -l | grep postdrop

-a always,exit -S all -F path=/usr/sbin/postdrop -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "postdrop" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/postdrop -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258200"><Rule severity="medium"><version>RHEL-09-654130</version><title>RHEL 9 must audit all uses of the postqueue command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "postqueue" command with the following command:

$ sudo auditctl -l | grep postqueue

-a always,exit -S all -F path=/usr/sbin/postqueue -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "postqueue" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/postqueue -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258201"><Rule severity="medium"><version>RHEL-09-654135</version><title>RHEL 9 must audit all uses of the ssh-agent command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "ssh-agent" command with the following command:

$ sudo auditctl -l | grep ssh-agent

-a always,exit -S all -F path=/usr/bin/ssh-agent -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-ssh

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "ssh-agent" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/ssh-agent -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-ssh

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258202"><Rule severity="medium"><version>RHEL-09-654140</version><title>RHEL 9 must audit all uses of the ssh-keysign command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "ssh-keysign" command with the following command:

$ sudo auditctl -l | grep ssh-keysign

-a always,exit -S all -F path=/usr/libexec/openssh/ssh-keysign -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-ssh

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "ssh-keysign" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/libexec/openssh/ssh-keysign -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-ssh

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258203"><Rule severity="medium"><version>RHEL-09-654145</version><title>RHEL 9 must audit all uses of the su command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "su" command with the following command:

$ sudo auditctl -l | grep '/usr/bin/su\b'

-a always,exit -S all -F path=/usr/bin/su -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-priv_change

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "su" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/su -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-priv_change

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258204"><Rule severity="medium"><version>RHEL-09-654150</version><title>RHEL 9 must audit all uses of the sudo command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "sudo" command with the following command:

$ sudo auditctl -l | grep '/usr/bin/sudo\b'

-a always,exit -S all -F path=/usr/bin/sudo -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=priv_cmd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "sudo" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/sudo -F perm=x -F auid&gt;=1000 -F auid!=unset -k priv_cmd

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258205"><Rule severity="medium"><version>RHEL-09-654155</version><title>RHEL 9 must audit all uses of the sudoedit command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "sudoedit" command with the following command:

$ sudo auditctl -l | grep /usr/bin/sudoedit

-a always,exit -S all -F path=/usr/bin/sudoedit -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=priv_cmd

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "sudoedit" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/sudoedit -F perm=x -F auid&gt;=1000 -F auid!=unset -k priv_cmd

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258206"><Rule severity="medium"><version>RHEL-09-654160</version><title>RHEL 9 must audit all uses of the unix_chkpwd command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "unix_chkpwd" command with the following command:

$ sudo auditctl -l | grep unix_chkpwd

-a always,exit -S all -F path=/usr/sbin/unix_chkpwd -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "unix_chkpwd" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/unix_chkpwd -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258207"><Rule severity="medium"><version>RHEL-09-654165</version><title>RHEL 9 must audit all uses of the unix_update command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "unix_update" command with the following command:

$ sudo auditctl -l | grep unix_update

-a always,exit -S all -F path=/usr/sbin/unix_update -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "unix_update" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/unix_update -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258208"><Rule severity="medium"><version>RHEL-09-654170</version><title>RHEL 9 must audit all uses of the userhelper command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "userhelper" command with the following command:

$ sudo auditctl -l | grep userhelper

-a always,exit -S all -F path=/usr/sbin/userhelper -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-unix-update

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "userhelper" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/userhelper -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-unix-update

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258209"><Rule severity="medium"><version>RHEL-09-654175</version><title>RHEL 9 must audit all uses of the usermod command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "usermod" command with the following command:

$ sudo auditctl -l | grep usermod

-a always,exit -S all -F path=/usr/sbin/usermod -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-usermod

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "usermod " command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/sbin/usermod -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-usermod

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258210"><Rule severity="medium"><version>RHEL-09-654180</version><title>RHEL 9 must audit all uses of the mount command.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "mount" command with the following command:

$ sudo auditctl -l | grep /usr/bin/mount

-a always,exit -S all -F path=/usr/bin/mount -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-mount

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records upon successful/unsuccessful attempts to use the "mount" command by adding or updating the following rule in "/etc/audit/rules.d/audit.rules":

-a always,exit -F path=/usr/bin/mount -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-mount

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258211"><Rule severity="medium"><version>RHEL-09-654185</version><title>Successful/unsuccessful uses of the init command in RHEL 9 must generate an audit record.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "init" command with the following command:

$ sudo auditctl -l | grep /usr/sbin/init

-a always,exit -S all -F path=/usr/sbin/init -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-init

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure the audit system to generate an audit event for any successful/unsuccessful uses of the "init" command by adding or updating the following rule in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/init -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-init

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258212"><Rule severity="medium"><version>RHEL-09-654190</version><title>Successful/unsuccessful uses of the poweroff command in RHEL 9 must generate an audit record.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "poweroff" command with the following command:

$ sudo auditctl -l | grep poweroff

-a always,exit -S all -F path=/usr/sbin/poweroff -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-poweroff

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure the audit system to generate an audit event for any successful/unsuccessful uses of the "poweroff" command by adding or updating the following rule in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/poweroff -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-poweroff

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258213"><Rule severity="medium"><version>RHEL-09-654195</version><title>Successful/unsuccessful uses of the reboot command in RHEL 9 must generate an audit record.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "reboot" command with the following command:

$ sudo auditctl -l | grep reboot

-a always,exit -S all -F path=/usr/sbin/reboot -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-reboot

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure the audit system to generate an audit event for any successful/unsuccessful uses of the "reboot" command by adding or updating the following rule in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/reboot -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-reboot

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258214"><Rule severity="medium"><version>RHEL-09-654200</version><title>Successful/unsuccessful uses of the shutdown command in RHEL 9 must generate an audit record.</title><check system="C-x"><check-content>Verify that RHEL 9 is configured to audit the execution of the "shutdown" command with the following command:

$ sudo cat /etc/audit/rules.d/* | grep shutdown

-a always,exit -S all -F path=/usr/sbin/shutdown -F perm=x -F auid&gt;=1000 -F auid!=-1 -F key=privileged-shutdown

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure the audit system to generate an audit event for any successful/unsuccessful uses of the "shutdown" command by adding or updating the following rule in the "/etc/audit/rules.d/audit.rules" file:

-a always,exit -F path=/usr/sbin/shutdown -F perm=x -F auid&gt;=1000 -F auid!=unset -k privileged-shutdown

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258215"><Rule severity="medium"><version>RHEL-09-654205</version><title>Successful/unsuccessful uses of the umount system call in RHEL 9 must generate an audit record.</title><check system="C-x"><check-content>Verify RHEL 9 generates an audit record for all uses of the "umount" and system call with the following command:

$ sudo auditctl -l | grep b32 | grep 'umount\b'

-a always,exit -F arch=b32 -S umount -F auid&gt;=1000 -F auid!=-1 -F key=privileged-umount

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure the audit system to generate an audit event for any successful/unsuccessful use of the "umount" system call by adding or updating the following rules in "/etc/audit/audit.rules" and adding the following rules to "/etc/audit/rules.d/perm_mod.rules" or updating the existing rules in files in the "/etc/audit/rules.d/" directory:

-a always,exit -F arch=b32 -S umount -F auid&gt;=1000 -F auid!=unset -k privileged-umount

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258216"><Rule severity="medium"><version>RHEL-09-654210</version><title>Successful/unsuccessful uses of the umount2 system call in RHEL 9 must generate an audit record.</title><check system="C-x"><check-content>To determine if the system is configured to audit calls to the umount2 system call, run the following command:

$ sudo auditctl -l | grep umount2

-a always,exit -F arch=b64 -S umount2 -F auid&gt;=1000 -F auid!=-1 -F key=privileged-umount
-a always,exit -F arch=b32 -S umount2 -F auid&gt;=1000 -F auid!=-1 -F key=privileged-umount

If no line is returned, this is a finding.</check-content></check><fixtext fixref="F-x">Configure the audit system to generate an audit event for any successful/unsuccessful use of the "umount2" system call by adding or updating the following rules in a file in "/etc/audit/rules.d".

-a always,exit -F arch=b32 -S umount2 -F auid&gt;=1000 -F auid!=unset -k privileged-umount
-a always,exit -F arch=b64 -S umount2 -F auid&gt;=1000 -F auid!=unset -k privileged-umount

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258217"><Rule severity="medium"><version>RHEL-09-654215</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/sudoers.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers" with the following command:

$ sudo auditctl -l | grep '/etc/sudoers[^.]'

-w /etc/sudoers -p wa -k identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /etc/sudoers -p wa -k identity

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258218"><Rule severity="medium"><version>RHEL-09-654220</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/sudoers.d/ directory.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers.d/" with the following command:

$ sudo auditctl -l | grep /etc/sudoers.d

-w /etc/sudoers.d/ -p wa -k identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/sudoers.d/".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /etc/sudoers.d/ -p wa -k identity

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258219"><Rule severity="medium"><version>RHEL-09-654225</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/group.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/group" with the following command:

$ sudo auditctl -l | egrep '(/etc/group)'  

-w /etc/group -p wa -k identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/group".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /etc/group -p wa -k identity

The audit daemon must be restarted for the changes to take effect.</fixtext></Rule></Group>
<Group id="V-258220"><Rule severity="medium"><version>RHEL-09-654230</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/gshadow.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/gshadow" with the following command:

$ sudo auditctl -l | egrep '(/etc/gshadow)' 

-w /etc/gshadow -p wa -k identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/gshadow".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /etc/gshadow -p wa -k identity

The audit daemon must be restarted for the changes to take effect.</fixtext></Rule></Group>
<Group id="V-258221"><Rule severity="medium"><version>RHEL-09-654235</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/opasswd.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/security/opasswd" with the following command:

$ sudo auditctl -l | egrep '(/etc/security/opasswd)' 

-w /etc/security/opasswd -p wa -k identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/security/opasswd".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /etc/security/opasswd -p wa -k identity

The audit daemon must be restarted for the changes to take effect.</fixtext></Rule></Group>
<Group id="V-258222"><Rule severity="medium"><version>RHEL-09-654240</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/passwd.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/passwd" with the following command:

$ sudo auditctl -l | egrep '(/etc/passwd)' 

-w /etc/passwd -p wa -k identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/passwd".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /etc/passwd -p wa -k identity

The audit daemon must be restarted for the changes to take effect.</fixtext></Rule></Group>
<Group id="V-258223"><Rule severity="medium"><version>RHEL-09-654245</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/shadow.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/etc/passwd with the following command:

$ sudo auditctl -l | egrep '(/etc/shadow)' 

-w /etc/shadow -p wa -k identity

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/shadow".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /etc/shadow -p wa -k identity

The audit daemon must be restarted for the changes to take effect.</fixtext></Rule></Group>
<Group id="V-258224"><Rule severity="medium"><version>RHEL-09-654250</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /var/log/faillock.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/faillock" with the following command:

$ sudo auditctl -l | grep /var/log/faillock

-w /var/log/faillock -p wa -k logins

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/faillock".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /var/log/faillock -p wa -k logins

The audit daemon must be restarted for the changes to take effect.

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-258225"><Rule severity="medium"><version>RHEL-09-654255</version><title>RHEL 9 must generate audit records for all account creations, modifications, disabling, and termination events that affect /var/log/lastlog.</title><check system="C-x"><check-content>Verify RHEL 9 generates audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/lastlog" with the following command:

$ sudo auditctl -l | grep /var/log/lastlog
 
-w /var/log/lastlog -p wa -k logins

If the command does not return a line, or the line is commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to generate audit records for all account creations, modifications, disabling, and termination events that affect "/var/log/lastlog".

Add or update the following file system rule to "/etc/audit/rules.d/audit.rules":

-w /var/log/lastlog -p wa -k logins

The audit daemon must be restarted for the changes to take effect.

$ sudo service auditd restart</fixtext></Rule></Group>
<Group id="V-279936"><Rule severity="medium"><version>RHEL-09-654097</version><title>RHEL 9 must audit any script or executable called by cron as root or by any privileged user.</title><check system="C-x"><check-content>Verify RHEL 9 is configured to audit the execution of any system call made by cron as root or by any privileged user.

$ sudo auditctl -l | grep /etc/cron.d
-w /etc/cron.d -p wa -k cronjobs

$ sudo auditctl -l | grep /var/spool/cron
-w /var/spool/cron -p wa -k cronjobs

If either of these commands do not return the expected output, or the lines are commented out, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to audit the execution of any system call made by cron as root or by any privileged user.

Add or update the following file system rules to "/etc/audit/rules.d/audit.rules":
-w /etc/cron.d/ -p wa -k cronjobs
-w /var/spool/cron/ -p wa -k cronjobs

To load the rules to the kernel immediately, use the following command:

$ sudo augenrules --load</fixtext></Rule></Group>
<Group id="V-258152"><Rule severity="medium"><version>RHEL-09-653015</version><title>RHEL 9 audit service must be enabled.</title><check system="C-x"><check-content>Verify the audit service is configured to produce audit records with the following command:

$ systemctl status auditd.service

auditd.service - Security Auditing Service
Loaded:loaded (/usr/lib/systemd/system/auditd.service; enabled; vendor preset: enabled)
Active: active (running) since Tues 2022-05-24 12:56:56 EST; 4 weeks 0 days ago

If the audit service is not "active" and "running", this is a finding.</check-content></check><fixtext fixref="F-x">To enable the auditd service run the following command:

$ sudo systemctl enable --now auditd</fixtext></Rule></Group>
<Group id="V-258153"><Rule severity="medium"><version>RHEL-09-653020</version><title>RHEL 9 audit system must take appropriate action when an error writing to the audit storage volume occurs.</title><check system="C-x"><check-content>Verify RHEL 9 takes the appropriate action when an audit processing failure occurs.

Check that RHEL 9 takes the appropriate action when an audit processing failure occurs with the following command:

$ sudo grep disk_error_action /etc/audit/auditd.conf

disk_error_action = HALT

If the value of the "disk_error_action" option is not "SYSLOG", "SINGLE", or "HALT", or the line is commented out, ask the system administrator (SA) to indicate how the system takes appropriate action when an audit process failure occurs. If there is no evidence of appropriate action, this is a finding.</check-content></check><fixtext fixref="F-x">Configure RHEL 9 to shut down by default upon audit failure (unless availability is an overriding concern).

Add or update the following line (depending on configuration "disk_error_action" can be set to "SYSLOG" or "SINGLE" depending on configuration) in "/etc/audit/auditd.conf" file:

disk_error_action = HALT

If availability has been determined to be more important, and this decision is documented with the information system security officer (ISSO), configure the operating system to notify SA staff and ISSO staff in the event of an audit processing failure by setting the "disk_error_action" to "SYSLOG".</fixtext></Rule></Group>
</Benchmark>
