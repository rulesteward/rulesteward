{
  "missed": 9,
  "total": 194,
  "note": "Hand-edited; the reason for every change lives in the commit body, never in a regenerate. Measured 2026-09-06 by the eight-shard CI run on PR #15 after strip_ansi was deleted: 179 caught, 9 missed, 5 unviable, 1 timeout. The 9 are comparison and boolean flips in parse, fields, is_corrupt_field_name, escaped and policy::rule; the timeout is a += mutation that stops unescape's byte-walking loop advancing."
}
