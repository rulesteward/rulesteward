{
  "missed": 0,
  "total": 527,
  "note": "Hand-edited; the reason for every change lives in the commit body, never in a regenerate. Measured by the sixteen-shard CI verdict on PR #126: 511 caught, 0 missed, 16 unviable, 0 timeouts of 527 generated. The run before it, 35469491349 on master, generated 529 with 5 missed and 2 timeouts: the 5 have tests in src/fapolicyd/parse.rs, and the 2 are the hangs excluded by name in .cargo/mutants.toml, so the denominator loses exactly those two and nothing else. A missed above 0 is now a regression rather than a ceiling to raise."
}
