RuleSteward -- THIRD-PARTY LICENSES
===================================

The RuleSteward release binary statically links the third-party Rust crates
listed below. Their licenses (predominantly MIT / Apache-2.0 / BSD) require that
the copyright notice and license text accompany binary redistribution; this file
satisfies that for the permissive set. Generated by cargo-about.

Handled separately (see the release assets / NOTICE), NOT listed here:
  * libsepol (statically linked, LGPL-2.1-or-later) -- LICENSES/LGPL-2.1.txt
    + LGPL-2.1-SOURCE-OFFER.txt
  * liblmdb  (statically linked, OLDAP-2.8)         -- LICENSES/OLDAP-2.8.txt
  * musl libc (statically linked, MIT) -- license text appended at the end of
    this file by the release workflow (musl is not a Cargo dependency, so
    cargo-about cannot enumerate it).

Overview ({{#each overview}}{{{name}}}: {{count}}{{#unless @last}}, {{/unless}}{{/each}})

{{#each licenses}}
================================================================================
{{{name}}} ({{{id}}})
--------------------------------------------------------------------------------
Used by:
{{#each used_by}}  * {{{crate.name}}} {{{crate.version}}}
{{/each}}
{{{text}}}

{{/each}}
