# The syslog_format line shipped in the fapolicyd package, verbatim. Only that key
# is read (DESIGN.md §6 step 2); the rest is here so the file looks like a real conf.
permissive = 0
syslog_format = rule,dec,perm,auid,pid,exe,:,path,ftype,trust
