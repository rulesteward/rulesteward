# Trap: Subsystem accumulates across DIFFERENT names but is first-value-wins for
# the SAME name. The second `sftp` definition is silently dropped by sshd.
Subsystem sftp /usr/libexec/openssh/sftp-server
Subsystem sftp /opt/other/sftp-server
