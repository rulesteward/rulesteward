# Trap: an absolute Include path that does not exist resolves to nothing. This
# absolute path is chosen to be absent on any host so the fixture is deterministic.
Include /nonexistent-rulesteward-e03-trap/missing.conf
