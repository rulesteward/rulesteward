# TRAP: Banner is missing (floor directive, required by RHEL8 V2R4 V-230225,
# RHEL9 V2R7 V-257981, RHEL10 V1R1 V-281224). W01 must fire.
# NOTE: the trap corpus driver uses SshdLintContext::default() (target=None),
# so this fixture exercises the conservative floor (14-directive RHEL8 set).
# Banner is in the floor set, so its absence fires W01 under any target.
PermitRootLogin no
StrictModes yes
X11Forwarding no
