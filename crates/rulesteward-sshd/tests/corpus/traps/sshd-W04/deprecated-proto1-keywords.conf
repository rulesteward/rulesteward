# Trap: legacy protocol-1 keywords deprecated since OpenSSH 7.6
# (openssh.org/txt/release-7.6). The daemon answers "Deprecated option <X>"
# on Rocky 8/9/10 (exit 0), confirming these are sshd-W04 (recognized-but-
# deprecated), NOT sshd-E01 (truly-unknown). Each occurrence fires one W04.
# Source: sshd-stig-version-grounding.md section 3, [VM] 2026-06-15T02:23Z.
RhostsRSAAuthentication no
RSAAuthentication yes
KeyRegenerationInterval 3600
ServerKeyBits 1024
