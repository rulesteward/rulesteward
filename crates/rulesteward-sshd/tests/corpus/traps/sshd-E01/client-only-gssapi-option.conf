# Trap: a client-side ssh_config(5) option pasted into the server config.
# GSSAPIClientIdentity is not an sshd_config keyword on any RHEL version (the
# daemon answers "Bad configuration option"), so sshd-E01 fires.
GSSAPIClientIdentity alice
