# Trap: a misspelled / nonexistent directive. sshd answers "Bad configuration
# option" and refuses to start, so this is a hard error the operator must see.
# ZZBogusDirective is unknown to every supported OpenSSH version, so sshd-E01
# fires under any --target (and with none, via the union).
ZZBogusDirective yes
