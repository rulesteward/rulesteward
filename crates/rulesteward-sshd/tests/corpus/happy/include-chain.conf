# Include-chain example: glob drop-in directory plus a specific extra file.
Include /etc/ssh/sshd_config.d/*.conf
Include /etc/ssh/extra.d/hardening.conf

PermitRootLogin no
PasswordAuthentication no
UsePAM yes
