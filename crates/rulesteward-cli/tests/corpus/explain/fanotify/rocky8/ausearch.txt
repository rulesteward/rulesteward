# ausearch -m FANOTIFY output (human format)
# Command: sudo ausearch -m FANOTIFY -ts recent
# Result: <no matches>
#
# NOTE: Rocky 8.10 (kernel 4.18.0-553.el8_10.x86_64, fapolicyd 1.3.2, audit 3.1.2)
# does NOT emit FANOTIFY audit events (type 1331) when fapolicyd denies execution.
# The kernel 4.18 on RHEL/Rocky 8 does not write FANOTIFY records to the audit log.
# fapolicyd IS enforcing (deny_audit rules present, /tmp/rs_untrusted exits 126),
# but the FANOTIFY audit event is never written to /var/log/audit/audit.log.
#
# grep -a 'type=FANOTIFY' /var/log/audit/audit.log => 0 matches (confirmed)
#
# This VM cannot produce real FANOTIFY audit records on this kernel/audit combination.
# See README.md for diagnostics.
