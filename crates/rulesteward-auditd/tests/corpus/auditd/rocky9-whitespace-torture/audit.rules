## This file is automatically generated from /etc/audit/rules.d
# Whitespace-torture (catalog #38): column-0 comments, inline #, blank lines,
# trailing whitespace, tabs vs spaces. All valid for augenrules/auditctl line
# parsing (each non-comment line is one rule; # begins a comment, f3 section 2).
# The parser must normalize whitespace and strip comments without mis-parsing.

   # an indented (non-column-0) comment with leading spaces
	# a tab-indented comment

-a always,exit -S execve -k execve_all   
	-a   always,exit    -S   openat   -F 'auid>=1000'   -F 'auid!=unset'   -k file_access
-w /etc/passwd -p wa -k passwd_watch		# inline comment after a tab

# trailing blank line below

