# Illegal: filetype is only legal on the exit filter list.
# user list -> EAU_EXITONLY (libaudit.c:1844-1845 v3.1.5).
# Live-confirmed: "filetype can only be used with exit filter list".
-a always,user -F filetype=file -k test-filetype-user
