{
  "id": "rocky8-xver-broad-interface-bait",
  "os": 8,
  "image": "fapolicyd8",
  "policyvers": 31,
  "profile": "adversarial",
  "adversarial": true,
  "intent": "THE HEADLINE over-broadening contract case (f4 section 2.3). A logrotate_t -> shadow_t:file { read getattr } denial whose audit2allow -R picks the broad auth_read_shadow() interface. The oracle PROVES RuleSteward's raw narrow allow is strictly narrower: no macro, no typeattribute, no perm padding, no unrelated etc_t.",
  "denials": [
    {
      "sdomain": "logrotate_t",
      "ttype": "shadow_t",
      "tclass": "file",
      "perms": [
        "read"
      ],
      "permissive": false,
      "kind": "TeAllowable"
    },
    {
      "sdomain": "logrotate_t",
      "ttype": "shadow_t",
      "tclass": "file",
      "perms": [
        "getattr"
      ],
      "permissive": false,
      "kind": "TeAllowable"
    }
  ],
  "expected_narrow_allow": [
    "allow logrotate_t shadow_t:file { getattr read };"
  ],
  "authoritative_category": "TeAllowable",
  "floor_label": "none",
  "_kinds_note": "authoritative_category in {TeAllowable, Constraint, Bounds} (libsepol/audit2why replay against policyvers 31). floor_label in {none, Permissive, MlsSuspected, RoleSuspected} (record-only heuristic).",
  "oracle": {
    "audit2why_category": "TeAllowable",
    "audit2why_detail": "Unknown - should be dontaudit'd by active policy. The underlying TE gap (logrotate_t -> shadow_t:file read/getattr) exists; el8 also ships a dontaudit for this access.",
    "policyvers": 31,
    "container": "fapolicyd8 (RHEL 8.10, selinux-policy-3.14.3-139.el8_10.2)"
  },
  "triage_expectation": {
    "should_suggest_allow": true,
    "should_decline_with_reason": null,
    "suspected_trap": "An impl that shells out to audit2allow -R (or replicates its refpolicy-default behavior) emits `auth_read_shadow(logrotate_t)` - a single innocuous-looking macro call that, once compiled, grants a policy-wide attribute + 3 extra perms on shadow_t + a full 6-perm grant on the unrelated etc_t:dir. This is precisely the over-broadening RuleSteward exists to refuse."
  },
  "xver_baseline": {
    "scenario": "rocky9-broad-interface-bait",
    "rocky9_bucket": "TeAllowable",
    "el8_bucket": "TeAllowable",
    "buckets_match": true,
    "policyvers_note": "Both el8 and el9 ship dontaudit for logrotate_t -> shadow_t:file read/getattr. The audit2why message and TeAllowable bucket are stable. Note: el8 policyvers 31 and el9 policyvers 33 produce same bucket and same audit2allow -N output."
  }
}