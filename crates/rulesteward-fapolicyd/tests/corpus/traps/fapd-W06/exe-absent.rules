# W06 fires: exe= (subject-side) value absent from trust DB and absent on disk.
# W06 checks both path= and exe= attributes; the absent exe= must trigger a diagnostic.
allow exe=/nonexistent/rs-trap/absent-exe : all
